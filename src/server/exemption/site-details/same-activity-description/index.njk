{% extends 'layouts/page.njk' %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "radio-page/macro.njk" import appRadioPage %}

{% block beforeContent %}
<nav>
  {{ govukBackLink({
    text: "Back",
    href: backLink
  }) }}
</nav>
{% endblock %}
{% block content %}

  {{ appRadioPage({
      name: "sameActivityDescription",
      errorSummary: errorSummary,
      errors: errors,
      heading: "Is the activity description the same for every site?",
      isPageHeading: true,
      value: payload.sameActivityDescription,
      errorMessage: errors['sameActivityDescription'],
      projectName: projectName,
      items: [
        {
          value: "yes",
          text: "Yes, the description is the same for every site",
          hint: {
            text: "You'll only need to enter the description once"
          }
        },
        {
          value: "no",
          text: "No, at least one site has a different description",
          hint: {
            text: "You'll need to enter a description for each site"
          }
        }
      ],
      csrfToken: csrfToken,
      cancelLink: "/exemption/task-list?cancel=site-details"
    })
  }}

{% endblock %}


